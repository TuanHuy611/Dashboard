
# Cài các thư viện cần thiết (chỉ chạy 1 lần)
pip install streamlit pandas plotly scikit-learn xgboost joblib

# Chạy dashboard
streamlit run app.py

# Các file train_valid_test chỉ dùng để xác định models nên chỉ cần các file còn lại để chạy 
